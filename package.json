{
  "name": "covertone",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "android:build": "pnpm build && pnpm exec cap sync && sh scripts/patch-aapt2.sh && cd android && ./gradlew assembleDebug",
    "android:release": "pnpm build && pnpm exec cap sync && sh scripts/patch-aapt2.sh && cd android && ./gradlew assembleRelease",
    "android:bundle": "pnpm build && pnpm exec cap sync && sh scripts/patch-aapt2.sh && cd android && ./gradlew bundleRelease",
    "android:open": "pnpm exec cap open android",
    "ios:add": "pnpm exec cap add ios",
    "ios:sync": "pnpm exec cap sync ios",
    "ios:open": "pnpm exec cap open ios",
    "ios:build": "pnpm build && pnpm exec cap sync ios && cd ios && xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' build 2>&1 || echo 'iOS builds require macOS with Xcode'",
    "icons:generate": "pnpm exec cap add android 2>/dev/null; pnpm exec cap sync 2>/dev/null; echo 'Run cap sync to regenerate platform resources'",
    "docker:build": "docker build -t covertone:latest .",
    "docker:run": "docker run -p 8080:80 covertone:latest",
    "lint": "prettier --check src/ && eslint src/ && pnpm exec svelte-check",
    "format": "prettier --write src/",
    "typecheck": "pnpm exec svelte-check",
    "lint:eslint": "eslint src/",
    "lint:format": "prettier --check src/",
    "lint:svelte": "pnpm exec svelte-check"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "@testing-library/svelte": "^5.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-svelte": "^3.0.0",
    "jsdom": "^26.0.0",
    "prettier": "^3.0.0",
    "prettier-plugin-svelte": "^3.3.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "svelte-eslint-parser": "^1.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.2",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/app": "^8.1.0",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1",
    "md5": "^2.3.0"
  }
}
